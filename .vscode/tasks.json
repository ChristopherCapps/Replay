{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "fake",
            "type": "shell",
            "args": ["build"],
            "group": "build",
            "presentation": {
                "reveal": "silent"
            },
            "problemMatcher": "$msCompile",
            "isBackground": false,
            "options": { 
                "cwd": "${workspaceFolder}",
                "env": {"vsCodeSession": "true"}
            }
        }
    ]
}